# Changelog

## 1.0.0

- Extracted the iOS splash branding config plugin into a standalone package.
- Added `plugin/src/withSplashScreenIosBrand.ts` source layout with compiled `plugin/build` output.
- Added package metadata and publishing-friendly exports for direct package consumption.
